---
title: "なぜ React 哲学を守らないといけないのか？"
emoji: "🎃"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["react", "哲学"]
published: false
---

こんにちは．[株式会社カミナシ](https://corp.kaminashi.jp/) でカミナシ従業員開発チーム a.k.a Negima チームの EM をしております [Keeth](https://x.com/kuwahara_jsri) こと桑原です．

---

こんにちは．[株式会社カミナシ](https://corp.kaminashi.jp/) でカミナシ従業員開発チーム a.k.a Negima チームの EM をしております [Keeth](https://x.com/kuwahara_jsri) こと桑原です．

毎日お世話になっている 𝕏 で以下のポストを見かけました．

https://x.com/le_panda_noir/status/1962638427628937481

しっかりリプライで

> 書きたいってか、こういう感じのを誰か書いて使わせてくれ
>

とあったので笑いましたが，自分なりにこれは整理してみたくなったので，色々調べつつ自分なりに考えて書いてみます．

---

## なぜ React 哲学を守らないといけないのか？

React は良くも悪くも，とても表現力が高い柔軟なライブラリで，哲学通りじゃない柔軟な書き方が割とできたりします．上記の投稿でクロパンダさん御本人も書かれているように，哲学に従っていないコードは破壊的変更等でバージョンアップにより互換性を失い，アプリケーションが壊れる可能性があります．自分も大小あれど経験したことがあります．

その中哲学を守ることは，アプリケーションを**予測可能で，バグりにくく，将来のReactの進化に乗り遅れないようにするため**という面が大きいです．

Reactは，UIを **`「宣言的」`** に（つまり「どうやるか」ではなく「どうあるべきか」を）記述するためのライブラリです．この根本思想に従うことで，コードはシンプルで理解しやすくなります．React 哲学は，この宣言的な UI 構築を支えるための「ルール」や「お作法」と言ってもよく，だからこそ守るべきだと自分は考えます．

## React哲学を守る = コンポーネントを「純粋」にする

React 哲学の核となる考え方は，**コンポーネントを純粋な関数のように保つこと**です．「純粋な関数」とは，**「同じ入力（props）が与えられれば、必ず同じ出力（JSX）を返す」**いう性質を持つ関数，つまり冪等性（Idempotency）を保つ関数のことを言います．

※冪等性：ある操作を1回行っても、10回行っても、結果は同じであるという性質のこと

例えば数学の`f(x) = 2x`という関数は、`x`が`2`なら必ず`4`を返される．

**純粋なコンポーネントのルール:**

1. **自分の仕事に集中する：** レンダリング中に、自分以外の変数やオブジェクトを変更しない，すなわち副作用を起こさない
2. **同じ材料からは同じものを作る：** `props, state, context`といった「公式な材料」だけを使ってJSXを生成する

これを守ることで，React はコンポーネントの表示を自由に最適化できます．例えば，props が変わっていなければ再計算をスキップしたり，バックグラウンドで先に計算しておいたりすることが可能になります．

## 具体的にバージョンアップで壊れる危険性のあるコード

冪等性を破っていると，将来の React の新機能によって意図しない動作を引き起こされる可能性があります．React は`useEffect`の中の処理がこの性質を持つことを期待しています．もう少し具体的に見ていきましょう．

### 壊れる可能性のあるコード例①: React 18 Strict Modeで問題発生

典型的なのが，**クリーンアップ関数を持たない`useEffect`** です．

```tsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    // APIを呼び出してカウンターを増やす副作用
    fetch(`/api/users/${userId}/increment-view-count`, { method: 'POST' });

    fetch(`/api/users/${userId}`)
      .then(res => res.json())
      .then(setUser);
  }, [userId]);

  return <div>{user?.name}</div>;
}
```

このコードは一見問題なく見えますが，「ユーザーがページを見た」というイベントを送信するこの処理は冪等ではありません．React 18 の Strict Mode では React が意図的に `useEffect` を2回実行するため，ページビューが2回カウントされてしまいます．妄想ですが，これが将来 production でも実行されるようになれば，閲覧回数の数字が期待値とは異なる結果になってしまいます．

さらに React が開発中の `Offscreen API` のような新機能は，この冪等性を前提としています．

::::message
**details Offscreen APIとは？**
ユーザーの目に見えない裏側で，UI をあらかじめレンダリングしておく機能です．例えばタブを切り替えたときに，次の画面が一瞬で表示されるような体験を実現します．
::::

この機能が有効になると，React はパフォーマンス向上のために **コンポーネントを画面に表示する前に裏でレンダリング（= Effectを実行）し，その後，不要と判断すればそれを破棄（= クリーンアップを実行）し，再度表示する（= 再度Effectの実行）**といった挙動をします。

先ほどの `UserProfile` コンポーネントがこの機能で使われると，**ユーザーが一度も見ていないのに，裏でのレンダリングだけでページビューが記録されてしまう**，というバグが発生します（たぶん）．これが「バージョンアップで壊れる」の具体例です．

### 正しいコード

`useEffect`には，その処理を「打ち消す」ためのクリーンアップ関数を常に意識します．

```tsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    let cancelled = false;

    // 副作用のない読み取り専用操作
    fetch(`/api/users/${userId}`)
      .then(res => res.json())
      .then(data => {
        if (!cancelled) {
          setUser(data);
        }
      });

    return () => {
      cancelled = true;
    };
  }, [userId]);

  // カウントアップは別のユーザーアクション時に実行
  const handleViewCount = () => {
    fetch(`/api/users/${userId}/increment-view-count`, { method: 'POST' });
  };

  return (
    <div onClick={handleViewCount}>
      {user?.name}
    </div>
  );
}
```

このコードでは React 哲学に準拠し，将来の機能でも安全に動作します（おそらく）．

このように，React の useEffect を正しく使うには，「一度きりの処理」をどう実現するかではなく， **「何度実行されても最終的な結果が正しくなるように処理を設計する」** という考え方が非常に重要になります．

### 壊れる可能性のあるコード例②: React Concurrent Featuresで問題発生

```jsx
let globalCounter = 0;

function ImpureComponent({ items }) {
  // レンダリング中にグローバル変数を変更
  globalCounter++;

  // 外部の状態を直接変更
  items.forEach(item => {
    item.processed = true;
  });

  return (
    <div>
      <p>Rendered {globalCounter} times</p>
      <ul>
        {items.map(item => (
          <li key={item.id}>{item.name}</li>
        ))}
      </ul>
    </div>
  );
}
```

こちらのコードでは，React 18 の Concurrent Rendering でレンダリングが中断・再開されてしまうため，純粋なコンポーネントではないと正しく動作しないかもしれません．また，グローバル変数の値が予測不能にもなります．さらに，props を直接変更することで，親コンポーネントが予期しないような結果になり，親への影響も起きるかもしれません．

### 正しいコード

```jsx
function PureComponent({ items }) {
  const [renderCount, setRenderCount] = useState(0);

  // 副作用はuseEffectで管理
  useEffect(() => {
    setRenderCount(prev => prev + 1);
  });

  // 元の配列を変更せずに新しい配列を作成
  const processedItems = items.map(item => ({
    ...item,
    processed: true
  }));

  return (
    <div>
      <p>Rendered {renderCount} times</p>
      <ul>
        {processedItems.map(item => (
          <li key={item.id}>{item.name}</li>
        ))}
      </ul>
    </div>
  );
}
```

## まとめ

React 哲学を守ることは，単に「ルールだから」という形式的なものではなく，React というライブラリが持つ能力を最大限に引き出し，将来の進化にも耐えうる，堅牢で予測可能な UI を構築するための最も合理的な方法なのだと考えます．